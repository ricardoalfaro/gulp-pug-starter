# Web Static Boilerplate with Pug, Sass, Gulp & BrowserSync

### Use
  - Node 7
  - Pug
  - SCSS
  - Gulp/Watch

### To run
- After clone repository, you must verified have node installed on your machine: `node -v` on CLI.
- An then run `npm install`
- Finally, execute `gulp`